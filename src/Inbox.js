import React from 'react';
import { HashRouter, Route } from 'react-router-dom';

export default class Inbox extends React.Component {
    constructor() {
        super();
    }

    render() {
        // const  { firstname, lastname, email, password } = this.state;
        //send info to Redux
        return <div className="inbox-main" />;
    }
}
